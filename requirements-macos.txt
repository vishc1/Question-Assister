# macOS-compatible requirements
# Note: Some Windows-specific features (screen capture exclusion, WASAPI) won't work on macOS

# UI Framework
customtkinter==5.2.1

# Audio (macOS-compatible)
pyaudio==0.2.14

# RAG Pipeline Dependencies
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.0.13
openai>=1.10.0
faiss-cpu>=1.9.0
pypdf>=4.0.0
python-docx>=1.1.0
tiktoken>=0.5.0
python-dotenv>=1.0.0

# Real-time Transcription & AI
deepgram-sdk==3.2.7
websockets==12.0
numpy>=1.26.0

# Note: These Windows-only packages are excluded:
# - pyaudiowpatch (Windows WASAPI loopback)
# - pywin32 (Windows API)
# - comtypes (Windows COM)
